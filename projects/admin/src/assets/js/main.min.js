"use strict";

window.addEventListener("DOMContentLoaded", function () {
  LazyLoadFunction();
  changeOpacity();
  // toggleModalChucNangAndSelect2();
  // eventRowTable('#them_chitiet_chuongtrinhdaotao');
  // eventRowTable('#them_chitiet_chitieudaotao');
  // eventRowTable('#danhsach_monhoc');
  // eventRowTable('#danhsach_khoabomon');
  // eventRowTable('#danhsach_chucvu');
  // eventRowTable('#danhsach_diemsinhvien');
  // eventRowTable('#danhsach_giaovien');
  // eventRowTable('#danhsach_lophoc');
  // eventRowTable('#danhsach_lophocphan');
  // eventRowTable('#danhsach_diemlophocphan');
  // eventRowTable('#danhsach_cotdiemlophocphan');
  // eventRowTable('#danhsach_sinhvienlophocphan');
  // eventRowTable('#danhsach_giaovienlophocphan');
  // eventRowTable('#danhsach_phanconggiangday');
  // eventRowTable('#danhsach_quanlykhaosat');
  // eventRowTable('#danhsach_tracuuthongtin');
  // eventRowTable('#danhsach_quanlybaidang');
  // eventRowTable('#danhsach_quanlygroup');
  // eventRowTable('#danhsach_quanlypage');
});

function LazyLoadFunction() {
  var lazyLoad = new LazyLoad({
    elements_selector: ".lazy",
  });
  lazyLoad.update();
}

function changeOpacity() {
  var opacity = parseFloat($("body").css("opacity"));

  if (opacity !== 1) {
    opacity += 0.01;
    $("body").css("opacity", opacity);
    setTimeout("changeOpacity()", 10);
  }
}

function toggleModalChucNangAndSelect2() {
  $(".sidebar_chucnang__item").click(function () {
    $(".modal_chucnang__select2").select2({
      dropdownParent: $(
        ".modal_chucnang__wrapper[data-modal=".concat(this.dataset.modal, "]")
      ),
    });
    $(
      ".modal_chucnang__wrapper[data-modal=".concat(this.dataset.modal, "]")
    ).addClass("show");
    $("body").addClass("overflow-hidden");
  }); // close modal with Escape key

  $(window).on("keyup", function (event) {
    var keycode = event.which || event.keyCode;

    if (keycode == 27 && $(".modal_chucnang__wrapper").hasClass("show")) {
      $(".modal_chucnang__wrapper").removeClass("show");
      $("body").removeClass("overflow-hidden");
    }
  }); // close modal

  $(".modal_chucnang__close").click(function () {
    $(this).parent().removeClass("show");
    $("body").removeClass("overflow-hidden");
  });
} //#region event Row Table into Modal Chuc Nang

function eventRowTable(idSelector) {
  addRowTable(idSelector);
  removeRowTable(idSelector);
  editRowTable(idSelector);
  saveRowTable(idSelector);
}

function addRowTable(idSelector) {
  var td = $("".concat(idSelector, " tfoot tr:first-child")).html();
  var tr = "<tr>".concat(td, " </tr>");
  $("".concat(idSelector, " .modal_chucnang__button__plus")).click(function () {
    $("".concat(idSelector, " tbody")).prepend(tr);
    editRowTable(idSelector);
    removeRowTable(idSelector);
  });
}

function removeRowTable(idSelector) {
  $("".concat(idSelector, " .modal_chucnang__button__delete")).click(
    function () {
      $(this).parents("tr").remove();
    }
  );
}

function editRowTable(idSelector) {
  $("".concat(idSelector, " .modal_chucnang__button__edit")).click(function () {
    $(this).parents("tr").find("input").removeAttr("disabled");
    $(this).parents("tr").find("select").removeAttr("disabled");
    $(this).parents("tr").find("textarea").removeAttr("disabled");
  });
}

function saveRowTable(idSelector) {
  $("".concat(idSelector, " .modal_chucnang__button__save")).click(function () {
    console.log("click");
    $("".concat(idSelector, " tbody input")).attr("disabled", true);
    $("".concat(idSelector, " tbody select")).attr("disabled", true);
    $("".concat(idSelector, " tbody textarea")).attr("disabled", true);
  });
} //#endregion
//# TTTH add mxh
$(document).on("click", ".add", function (event) {
  event.preventDefault();
  var count = parseInt($("#parent").attr("edu-card")) + 1;
  var html =
    '<div  class="col-3" style="margin-top: 2em;" id="edu-card-list-' +
    count +
    '"><div class="form-group"><input type="text" class="form-control"><small class="form-text text-muted">Icon</small><input type="text" class="form-control"><small class="form-text text-muted">Link</small></div><div class="btn btn-danger remove">Xóa</div></div>';
  $("#parent").append(html);
  $("#parent").attr("edu-card", count);
});
$(document).on("click", ".remove", function (event) {
  event.preventDefault();
  $(this).parent().remove();
});
//# sourceMappingURL=main.min.js.map
